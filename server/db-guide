DB name: PERSONAL_BUDGET
Table name: BUDGETS
Columns: USER (Varchar(100))
         MONTH_YEAR (Varchar(20)): set up like 1_2020
         TITLE (Varchar(50))
         TOTAL (Decimal)
         AMOUNT_USED (Decimal)

Views:
  Budgets as percentage of whole (pie)
  Used vs unused (total, as pie)
  Used vs unused (each, as bar)
