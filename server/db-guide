DB name: PERSONAL_BUDGET
Table name: BUDGETS
Columns: USER (Varchar(100))
         MONTH_YEAR (Varchar(20)): set up like JAN_2020
         TITLE (Varchar(50))
         TOTAL (Decimal)
         AMOUNT_USED (Decimal)
